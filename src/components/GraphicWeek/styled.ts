import styled from 'styled-components';

export const GraphicWeekContainer = styled.div`

    background-color: #FFFFFF;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0px 5px 7px 5px rgba(0, 0, 0, 0.10);

    .dashboard-graphicWeek-info {
        display: flex;
        align-items: center;
        margin-bottom: 20px;

        h3 {
            font-size: 18px;
            color: #313131;
            font-family: 'Roboto', sans-serif;
            font-weight: 600;
        }
        span {
            margin-left: auto;
            font-size: 14px;
            font-family: 'Roboto', sans-serif;
            color: #313131;
            opacity: 0.9;
        }
    }

`